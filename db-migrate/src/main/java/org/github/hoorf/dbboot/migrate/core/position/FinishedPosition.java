package org.github.hoorf.dbboot.migrate.core.position;

public class FinishedPosition implements MigratePosition<FinishedPosition> {
    @Override
    public int compareTo(FinishedPosition o) {
        return 0;
    }
}
